const a = 56
const numbers = [56, 3, 5]

const person = {
    name: 'John',
}

const message = `Hi, ${person.name} has a: ${a} access to ${numbers}`
console.log(message)

const squre = a =>  a * a
console.log(squre(3))